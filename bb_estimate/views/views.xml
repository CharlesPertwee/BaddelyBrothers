<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="bb_estimate.list">
      <field name="name">BbEstimate list</field>
      <field name="model">bb_estimate.bb_estimate</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="bb_estimate.action_window">
      <field name="name">BbEstimate window</field>
      <field name="res_model">bb_estimate.bb_estimate</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="bb_estimate.action_server">
      <field name="name">BbEstimate server</field>
      <field name="model_id" ref="model_bb_estimate_bb_estimate"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="BbEstimate" id="bb_estimate.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="bb_estimate.menu_1" parent="bb_estimate.menu_root"/>
    <menuitem name="Menu 2" id="bb_estimate.menu_2" parent="bb_estimate.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="bb_estimate.menu_1_list" parent="bb_estimate.menu_1"
              action="bb_estimate.action_window"/>
    <menuitem name="Server to list" id="bb_estimate" parent="bb_estimate.menu_2"
              action="bb_estimate.action_server"/>
    -->
  </data>
</odoo>