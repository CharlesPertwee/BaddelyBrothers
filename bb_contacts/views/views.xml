<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="bb_contacts.list">
      <field name="name">BbContacts list</field>
      <field name="model">bb_contacts.bb_contacts</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="bb_contacts.action_window">
      <field name="name">BbContacts window</field>
      <field name="res_model">bb_contacts.bb_contacts</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="bb_contacts.action_server">
      <field name="name">BbContacts server</field>
      <field name="model_id" ref="model_bb_contacts_bb_contacts"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="BbContacts" id="bb_contacts.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="bb_contacts.menu_1" parent="bb_contacts.menu_root"/>
    <menuitem name="Menu 2" id="bb_contacts.menu_2" parent="bb_contacts.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="bb_contacts.menu_1_list" parent="bb_contacts.menu_1"
              action="bb_contacts.action_window"/>
    <menuitem name="Server to list" id="bb_contacts" parent="bb_contacts.menu_2"
              action="bb_contacts.action_server"/>
    -->
  </data>
</odoo>